import { css, StyledFunction, StyledProps } from "styled-components";



/*export const mobile = (Props?: any ) => {
  return css`
    @media only screen and (max-width: 380px) {
      ${Props}
    }
  `;
};*/
